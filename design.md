# design.md

Placeholder. Real design language lives here later.

When filled in, this file should drive:

- Color palette (no AI-vibe gradients)
- Typography scale + font choices
- Spacing scale
- Components: header/nav, post list, post page, code blocks, footer
- Dark/light mode rules (if any)
- Accessibility constraints

For now the site uses minimal neutral styles in `src/styles/global.css` — intentional, to be replaced.
